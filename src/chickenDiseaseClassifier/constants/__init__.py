# to read the yaml file i am specifying in constant file 
# constants file is used for those files which will only be read once and will never be chnaged.

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAM_FILE_PATH = Path("params.yaml")
